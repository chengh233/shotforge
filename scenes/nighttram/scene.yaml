# 场景库条目 —— 一个可复用的"布景"。剧本里用 `scene: nighttram` 选它。
# 和 characters/ 并列：characters=演员(谁)，scenes=布景(在哪)。
name: 末班电车车厢
# description：英文（出图时辅助提示词）。
description: "interior of a quiet late-night tram carriage, Kyoto Animation (KyoAni) anime style, warm dim ceiling lights, rows of empty seats, hanging handrail straps, large dark windows reflecting the night"
# ref：一张该场景的参考图（空车厢即可），竖构图 9:16。
#   生成一张：在 nano banana 里用 description 出一张空车厢图（9:16），存成下面这个文件。
#   出帧时它会作为"布景参考图"喂给每一镜，锁住同一节车厢 + 画风 + 画幅。
ref: ref.png
